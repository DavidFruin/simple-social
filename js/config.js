// config.js - Client-side feature configuration

const Config = {
  // Append a relative label like "(just now)"/"(today)"/"(this week)" after
  // timestamps. Set to false to show the raw timestamp only.
  showRelativeTimeLabel: false
};

window.Config = Config;
